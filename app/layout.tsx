import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Interactive Tales - Choose Your Adventure",
  description: "An immersive interactive storytelling experience where every choice shapes your destiny.",
  keywords: "interactive story, choose your adventure, storytelling, narrative game",
  authors: [{ name: "Interactive Tales" }],
  viewport: "width=device-width, initial-scale=1",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
